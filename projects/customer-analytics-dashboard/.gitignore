# Large derived files regenerated by python/*.py — not committed to keep the
# repo lean. Run download_data.py, then clean_and_analyze.py and
# build_database.py to regenerate locally.
data/raw/online_retail_raw.xlsx
data/cleaned/online_retail_clean.csv
data/cleaned/customer_analytics.db
